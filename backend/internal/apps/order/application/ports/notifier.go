package ports

// (Reserved) Notifier interface — currently usecase uses email.Service directly
// for simplicity. Switch to this interface if/when we need to mock/swap in tests.
//
// Kept as a placeholder so future PRs can introduce the abstraction without
// touching the package layout.
